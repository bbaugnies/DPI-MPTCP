// This file was automatically generated by bifcl from /home/benjamin/Bro/bro-2.3.1/src/file_analysis/analyzer/extract/events.bif (plugin mode).


#include "events.bif.h"

EventHandlerPtr file_extraction_limit; 
void BifEvent::generate_file_extraction_limit(analyzer::Analyzer* analyzer, Val* f, Val* args, bro_uint_t limit, bro_uint_t offset, bro_uint_t len)
	{
	// Note that it is intentional that here we do not
	// check if ::file_extraction_limit is NULL, which should happen *before*
	// BifEvent::generate_file_extraction_limit is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(f);
	vl->append(args);
	vl->append(new Val(limit, TYPE_COUNT));
	vl->append(new Val(offset, TYPE_COUNT));
	vl->append(new Val(len, TYPE_COUNT));

	mgr.QueueEvent(::file_extraction_limit, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr);
	} // event generation
