// This file was automatically generated by bifcl from /home/benjamin/Bro/bro-2.3.1/src/analyzer/protocol/ntp/events.bif (plugin mode).


#include "events.bif.h"

EventHandlerPtr ntp_message; 
void BifEvent::generate_ntp_message(analyzer::Analyzer* analyzer, Connection* u, Val* msg, StringVal* excess)
	{
	// Note that it is intentional that here we do not
	// check if ::ntp_message is NULL, which should happen *before*
	// BifEvent::generate_ntp_message is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(u->BuildConnVal());
	vl->append(msg);
	vl->append(excess);

	mgr.QueueEvent(::ntp_message, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, u);
	} // event generation
