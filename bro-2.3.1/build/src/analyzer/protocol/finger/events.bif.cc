// This file was automatically generated by bifcl from /home/benjamin/Bro/bro-2.3.1/src/analyzer/protocol/finger/events.bif (plugin mode).


#include "events.bif.h"

EventHandlerPtr finger_request; 
void BifEvent::generate_finger_request(analyzer::Analyzer* analyzer, Connection* c, int full, StringVal* username, StringVal* hostname)
	{
	// Note that it is intentional that here we do not
	// check if ::finger_request is NULL, which should happen *before*
	// BifEvent::generate_finger_request is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(c->BuildConnVal());
	vl->append(new Val(full, TYPE_BOOL));
	vl->append(username);
	vl->append(hostname);

	mgr.QueueEvent(::finger_request, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, c);
	} // event generation
EventHandlerPtr finger_reply; 
void BifEvent::generate_finger_reply(analyzer::Analyzer* analyzer, Connection* c, StringVal* reply_line)
	{
	// Note that it is intentional that here we do not
	// check if ::finger_reply is NULL, which should happen *before*
	// BifEvent::generate_finger_reply is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(c->BuildConnVal());
	vl->append(reply_line);

	mgr.QueueEvent(::finger_reply, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, c);
	} // event generation
