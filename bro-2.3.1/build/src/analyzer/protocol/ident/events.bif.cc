// This file was automatically generated by bifcl from /home/benjamin/Bro/bro-2.3.1/src/analyzer/protocol/ident/events.bif (plugin mode).


#include "events.bif.h"

EventHandlerPtr ident_request; 
void BifEvent::generate_ident_request(analyzer::Analyzer* analyzer, Connection* c, PortVal* lport, PortVal* rport)
	{
	// Note that it is intentional that here we do not
	// check if ::ident_request is NULL, which should happen *before*
	// BifEvent::generate_ident_request is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(c->BuildConnVal());
	vl->append(lport);
	vl->append(rport);

	mgr.QueueEvent(::ident_request, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, c);
	} // event generation
EventHandlerPtr ident_reply; 
void BifEvent::generate_ident_reply(analyzer::Analyzer* analyzer, Connection* c, PortVal* lport, PortVal* rport, StringVal* user_id, StringVal* system)
	{
	// Note that it is intentional that here we do not
	// check if ::ident_reply is NULL, which should happen *before*
	// BifEvent::generate_ident_reply is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(c->BuildConnVal());
	vl->append(lport);
	vl->append(rport);
	vl->append(user_id);
	vl->append(system);

	mgr.QueueEvent(::ident_reply, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, c);
	} // event generation
EventHandlerPtr ident_error; 
void BifEvent::generate_ident_error(analyzer::Analyzer* analyzer, Connection* c, PortVal* lport, PortVal* rport, StringVal* line)
	{
	// Note that it is intentional that here we do not
	// check if ::ident_error is NULL, which should happen *before*
	// BifEvent::generate_ident_error is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(c->BuildConnVal());
	vl->append(lport);
	vl->append(rport);
	vl->append(line);

	mgr.QueueEvent(::ident_error, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, c);
	} // event generation
