// This file was automatically generated by bifcl from /home/benjamin/Bro/bro-2.3.1/src/analyzer/protocol/file/events.bif (plugin mode).


#include "events.bif.h"

EventHandlerPtr file_transferred; 
void BifEvent::generate_file_transferred(analyzer::Analyzer* analyzer, Connection* c, StringVal* prefix, StringVal* descr, StringVal* mime_type)
	{
	// Note that it is intentional that here we do not
	// check if ::file_transferred is NULL, which should happen *before*
	// BifEvent::generate_file_transferred is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(c->BuildConnVal());
	vl->append(prefix);
	vl->append(descr);
	vl->append(mime_type);

	mgr.QueueEvent(::file_transferred, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, c);
	} // event generation
