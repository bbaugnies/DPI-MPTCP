// This file was automatically generated by bifcl from /home/benjamin/Bro/bro-2.3.1/src/analyzer/protocol/interconn/events.bif (plugin mode).


#include "events.bif.h"

EventHandlerPtr interconn_stats; 
void BifEvent::generate_interconn_stats(analyzer::Analyzer* analyzer, Connection* c, Val* os, Val* rs)
	{
	// Note that it is intentional that here we do not
	// check if ::interconn_stats is NULL, which should happen *before*
	// BifEvent::generate_interconn_stats is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(c->BuildConnVal());
	vl->append(os);
	vl->append(rs);

	mgr.QueueEvent(::interconn_stats, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, c);
	} // event generation
EventHandlerPtr interconn_remove_conn; 
void BifEvent::generate_interconn_remove_conn(analyzer::Analyzer* analyzer, Connection* c)
	{
	// Note that it is intentional that here we do not
	// check if ::interconn_remove_conn is NULL, which should happen *before*
	// BifEvent::generate_interconn_remove_conn is called to avoid unnecessary Val
	// allocation.

	val_list* vl = new val_list;

	vl->append(c->BuildConnVal());

	mgr.QueueEvent(::interconn_remove_conn, vl, SOURCE_LOCAL, analyzer->GetID(), timer_mgr, c);
	} // event generation
